overwrite: true
schema: 'https://api.thegraph.com/subgraphs/name/orionswap/trading-data'
documents:
  - './utils/apollo/queries.ts'
generates:
  ./utils/generated/subgraph.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      declarationKind: 'interface'
      immutableTypes: true
